VITE_API_URL=http://localhost:3001/api

# Error monitoring (optional). Leave blank to disable Sentry entirely — the SDK
# is only loaded when a DSN is present.
VITE_SENTRY_DSN=
# Fraction of transactions traced when Sentry is enabled (0.0 - 1.0).
VITE_SENTRY_TRACES_RATE=0.1
